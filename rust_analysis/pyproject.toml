[build-system]
requires    = ["maturin>=1.4,<2.0"]
build-backend = "maturin"

[project]
name            = "qcd_analysis"
requires-python = ">=3.8"
dynamic         = ["version"]

[tool.maturin]
features = ["pyo3/extension-module"]
