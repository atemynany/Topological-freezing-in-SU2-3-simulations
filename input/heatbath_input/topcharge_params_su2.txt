# ==============================================================================
# SU(2) Topological Charge Parameters  —  used by run_topcharge_scan.sh
# ==============================================================================
# Applied to every ensemble config found in the results directory
# exclude_boundary_slices is auto-set by the scan script:
#   periodic dirs -> 0,  open dirs -> exclude_boundary_slices_open

# Configuration range
start_conf          100         # First config to measure (must match save_interval)
end_conf            10000       # Last config to measure
conf_step           100         # Step between configs (must match save_interval)

# APE smearing
smear_steps         15          # Total smearing steps applied before measuring Q
smear_interval      15          # Output Q every N smearing steps
smear_alpha         0.3         # APE smearing parameter alpha

# Boundary exclusion (applied only to open BC runs)
exclude_boundary_slices_open 40  # Time slices excluded per boundary for open BC
# ==============================================================================
